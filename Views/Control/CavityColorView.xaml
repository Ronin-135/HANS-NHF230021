<UserControl x:Class="WPFMachine.Views.Control.CavityColorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFMachine.Views.Control"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Template>
        <ControlTemplate TargetType="local:CavityColorView">
     
        <Border
            BorderBrush="Gray"
            BorderThickness="2"
            Margin="0,6,0,0"
            Style="{DynamicResource OvenState}">
                    <local:CollectionPallet Plts="{TemplateBinding Plts}" />
         </Border>

        </ControlTemplate>
    </UserControl.Template>
</UserControl>
